/** @format */

import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    margin: 15,
    flex: 1,
  },
  searchBtn: {
    position: 'absolute',
    bottom: 0,
    width: 80,
    height: 40,
    borderWidth: 2,
    borderRadius: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
